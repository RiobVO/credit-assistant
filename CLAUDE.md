# CLAUDE.md

> Читай этот файл ПОСЛЕ PROJECT_BRIEF.md. Здесь — текущий статус проекта и рабочие соглашения.

---

## Current Status

**Phase:** 3 закрыта. **Parser best-effort refactor merged в main** (`64444d0`) — закрывает CA-014 на уровне контракта парсера. Parser hardening (CA-011..014 первой итерации) — squash `75ab42b`. Phase 3.C — PR #5 (`936e1ac`). Phase 3.B — PR #4 (`317884f`). Phase 3.A — PR #3 (`5f3c5b8`). Phase 2 закрыта в части автоматических адаптеров; 2.6 (E2E на 5 фирмах) — pending данные от папы. **Готовы стартовать Phase 4 (Bank Mode UI).**
**Last completed task:** **parser hardening перед Phase 4** (squash в main `75ab42b`). 4 атомарных fix'а в одном коммите: **CA-011** ₽ → «сум» в `formatBigUzs` (web/.../dossier/[id]/_lib/format.ts) — совпадает с PDF-фильтром `fmt_uzs`. **CA-012** двухуровневый детект VAT-деклараций в `format_detector.py` для семейства 10006_* (10006_41/45/47 …): widened substring-scan по верхней зоне list01 (rows 1-20, cols A-F, case-insensitive) + structural fallback (list02 G6 + list04 G7 числа). VAT_REGISTRY проверяется до VAT_DECLARATION чтобы widened scan не ловил «Расчёт НДС» в подзаголовке ilova. Парсер `vat_declaration_parser.py` не тронут — уточним под структурные отличия 10006_45/47, когда будут живые файлы. **CA-013** `getYearsRange(15)` helper в `web/.../manual-input/_lib/years.ts` заменил хардкод `[2024,2025,2026]` в SoliqUpload (default год = текущий). **CA-014** tolerant parsing в `vat_registry_parser._read_rows` — row-level `MalformedXltxError` теперь WARN-лог `xltx.row_skipped sheet/row/reason` через stdlib logging + skip; `VatRegistryData.skipped_rows_count` для аудита; structural ошибки (`UnsupportedFormatError`, отсутствие листов) по-прежнему пробрасываются. Verify: ruff + mypy --strict + **428 unit** (+8: 4 на детектор + 4 на tolerant) + 23 integration (5 PDF-тестов skip на Windows) + tsc + eslint + next build — всё зелёное.
**Next task:** **Phase 4 (Bank Mode UI)** или **Phase 2.6 (E2E на 5 фирмах папы)** — выбор за пользователем. Открытое: TODO[CA-010] bundle TTF Inter+JetBrains Mono в `infrastructure/reports/pdf/fonts/` (косметика, v1 PDF работает на DejaVu Sans). TODO[CA-015]: уточнить парсер vat_declaration_parser.py под живые файлы 10006_45/10006_47 — детектор их уже опознаёт, но cells могут быть смещены.

**Phase 2 декомпозиция (статус):** 2.0 ✓ → 2.1 ✓ → 2.2 ✓ → 2.4.1 ✓ (API) → 2.4.2 ✓ (UI) → 2.5 persistence ✓ (PR #1) → **2.3 SoliqXltxAdapter ✓ (PR #2)** (Day 1 parsers + Day 2 domain + Day 3 API+UI) → 2.6 E2E на 5 фирмах ⏸ (pending: данные 5 фирм).

**Phase 3 декомпозиция (статус):** **3.A Result Screen UI ✓ (PR #3)** (mock data) → **3.B backend GET endpoint + KPI calc + frontend useQuery ✓ (PR #4)** → **3.C PDF endpoint ✓ (PR #5 ready)** (WeasyPrint+Jinja2+matplotlib, backend в Docker compose-сервисе `api`, см. ADR 0008).

**Открытые TODO:**
- TODO[CA-003]: реальный лукап ГНК для ИНН (сейчас pill «Проверено в ГНК» по 9-значной валидации формата)
- TODO[CA-006]: убрать дублирующий `ix_borrowers_inn` — `UniqueConstraint("inn")` уже создаёт индекс `borrowers_inn_key`. Косметика, отдельной миграцией перед production deploy.
- TODO[CA-010]: bundle TTF Inter (400/500/600/700) + JetBrains Mono (500/600) в `src/infrastructure/reports/pdf/fonts/` + `@font-face` в шаблоне. Сейчас PDF-шаблон использует fallback на DejaVu Sans (читается, но не совпадает с Inter на экране). Косметика, не блокер.
- TODO[CA-015]: уточнить парсер `vat_declaration_parser.py` под живые xltx 10006_45 / 10006_47 — детектор их уже опознаёт через widened sentinel + structural fallback (CA-012), но cells F6/G6/F7-F11/G7 могут быть смещены. Открыто до получения реальных файлов от папы / банка.

**Закрытые TODO:**
- TODO[CA-004] ✓ закрыт в Phase 3.A (per-year taxes UI на Шаге 2: `taxesPaid23/24/25` + проброс в `_form-mapper.ts`).
- TODO[CA-005] ✓ закрыт в 2.5.5 (`loan_request: LoanRequest` VO end-to-end).
- TODO[CA-007] ✓ закрыт в коммите `18eeb74` (hydration mismatch на `caseId` в `PageHead` — `useSyncExternalStore` с null-снапшотом на сервере).
- TODO[CA-008] ✓ закрыт в Phase 3.B.4 (удалён dead code `manual-input/_components/dossier-result.tsx`).
- TODO[CA-009] ✓ закрыт в Phase 3.B.4 (KPI shape в `lib/api.ts:DossierViewDto` теперь зеркалит реальный backend GET endpoint; `mock-dossier.ts` удалён).

**Активная ветка:** `main` (HEAD `75ab42b`). Parser hardening смержен squash-коммитом, ветка `fix/parser-hardening` удалена локально и в origin. Phase 4 стартует с новой ветки `feat/phase-4-...` от main.

**Договорённости по Phase 2 (зафиксированы):**
- VAT хранится отдельно от ЭСФ — изначально на агрегате `BorrowerSnapshot.esf_seller_vat_total` (ADR 0004). **2.3 Day 2 миграция:** одиночный агрегат заменён на `BorrowerSnapshot.vat_periods: list[VatPeriodReport]` (ADR 0006, partial supersede ADR 0004). Реальные данные my3.soliq.uz помесячные; сравнение декларация vs ЭСФ происходит в рамках одного налогового периода. Источник полей: декларация даёт `vat_declared` (стр.0101 list02 = `vat_charged_total` парсера), ilova даёт `esf_seller_vat_total` (sum НДС по продажам в реестре). Числа сходятся между источниками до копейки.
- ИНН заёмщика приходит явно от пользователя (через UI/API), не угадывается из имени файла.
- Реальные данные папы (полный CSV 25k строк + 5 xltx-форм) — локально в `~/Downloads`/`tests/fixtures/**/*_full.*`, не в git. В repo только `*_sample.csv`-фикстуры и synthetic factory-helpers для xltx (`tests/fixtures/soliq_xltx/_factories.py` — конструируют workbooks in-memory, без xltx-бинарников в репозитории).
- **xltx форматы (5 типов из my3.soliq.uz):** VAT_DECLARATION (8 листов), VAT_REGISTRY_ILOVA (10 листов, Приложение №4), FORM_2_INCOME_STATEMENT (3 листа), FORM_1_BALANCE_SHEET (4 листа), PROFIT_TAX (15 листов). Distinguished по сигнатурным cells list01.
- **Persistence (2.5):** testcontainers + real Postgres для интеграционных тестов; draft TTL = 30 дней (флаг `DRAFT_TTL_DAYS`); draft auth = по `draft_id` без owner (переделать в Phase 4 Bank Mode); CA-005 (loan term/rate/purpose/category) делается в 2.5.5 вместе с wire endpoint.
- **Compose-postgres** слушает host **5433** (5432 занят локальным нативным Postgres).
- **Windows + asyncpg:** обязателен `WindowsSelectorEventLoopPolicy` — дефолтный ProactorEventLoop рвёт коннекты. Уже настроено в `migrations/env.py`; для рантайма API фикс понадобится в 2.5.5 (uvicorn startup).

---

## Working Agreements

- Перед каждой задачей: перечитай PROJECT_BRIEF.md Section 4 (Architecture) и Section 11 (Anti-patterns)
- Plan mode обязателен если затрагивается >2 файлов
- Не начинай кодить без плана — сначала покажи декомпозицию
- Язык UI: русский. Язык кода: английский
- Коммиты: conventional commits (`feat:`, `fix:`, `refactor:`, `test:`, `docs:`, `chore:`)
- Никаких `TODO` без ID (`# TODO[CA-001]: описание`)

---

## Architecture Reminders

- `domain/` не знает про `infrastructure/` — никогда
- Все бизнес-правила — только в `domain/rules/`, ссылка на источник обязательна
- Новый банк = новый adapter, не правки в ядре
- Two modes (Bank / Accountant) — два UI поверх одного бизнес-ядра

---

## Security Hard Rules

- Данные заёмщиков не логируются
- Никаких внешних API в production (только on-premise)
- Soliq данные — только через официальный экспорт/API, не scraping
- `.env` не в git, secrets через Vault в production

---

## Start of Session Command

```
Прочитай @PROJECT_BRIEF.md целиком, потом @CLAUDE.md.
Скажи на каком phase мы сейчас и какая следующая atomic задача.
Не начинай кодить — сначала покажи план.
```

---

## Session Log

| Session | Phase | Completed | Notes |
|---------|-------|-----------|-------|
| 2026-05-08 | 0 | Foundation 0.1–0.9 | Stack: Python 3.12 + uv, FastAPI 0.136, Next 16.2 (вместо 15 — см. ADR 0002), shadcn/ui, TanStack Query, Postgres 16 + Redis 7 в compose, CI с ruff/mypy/pytest и eslint/tsc/build. |
| 2026-05-08 | 0 | Phase 0 follow-up | Compose поднят и здоров (`credit-postgres` + `credit-redis` healthy, `pg_isready` accepting, redis `PONG`). GitHub remote `origin` → `github.com/RiobVO/credit-assistant`, main pushed (HEAD `ba401fb`). uv добавлен в User PATH через installer — в новых сессиях работает без хака. |
| 2026-05-08 | 1 | Domain Core 1.0–1.9 | Branch `feat/phase-1-domain`. 4 value objects (INN/Money/DateRange/FlagSeverity), 8 entities, 17 правил pure fn, ScoringService (LOW=1/MED=3/HIGH=7/CRIT=15; <15 APPROVE, 15-29 REVIEW, ≥30 REJECT), YAML+loader, 5 синтетических borrowers, 217 тестов, coverage `src/domain/rules` ≈99%, ADR 0003. TODO: CA-001 (INN checksum ГНК), CA-002 (full graph CIRCULAR_INVOICING). |
| 2026-05-08 | 2 | 2.0 Domain под реальный CSV | Убран `Invoice.vat_amount`; добавлен `BorrowerSnapshot.esf_seller_vat_total: Money \| None`; `VAT_ESF_MISMATCH` переписан под агрегат, degraded mode без VAT-адаптера. ADR 0004. 218 тестов passed, ruff + mypy --strict зелёные. Изменения: `src/domain/entities/{invoice,borrower_snapshot}.py`, `src/domain/rules/financial/vat_esf_mismatch.py`, тесты + `tests/fixtures/synthetic_borrowers.py`. |
| 2026-05-08 | 2 | 2.1 Port + use case | `application/dto/parsed_data_chunk.py` (3 chunk-DTO + union), `application/ports/data_source_port.py` (`DataSourcePort` Protocol), `application/use_cases/build_borrower_snapshot.py` (+ `ChunkBorrowerMismatchError`). 13 use case-тестов; всего 231 passed; ruff + mypy --strict зелёные. |
| 2026-05-08 | 2 | 2.2 EsfCsvAdapter | `infrastructure/adapters/esf_csv/{parser,errors}.py` для e-factura.uz CSV (cp1251, `;`). Регекс на дату устойчив к 3 форматам номера; десятичные суммы с запятой; ПИНФЛ-покупатели; пустой ИНН → skip. Sample-фикстура (40 строк) в repo, полный 25k файл в `.gitignore`. Sanity check: парсер усваивает 25 292 invoices папы за 2020-01–2026-05. 22 теста; всего 254 passed; ruff + mypy --strict зелёные. Удалён placeholder `esf_json/`. |
| 2026-05-08 | 2 | 2.4.1 ManualInput API | `interfaces/api/shared/dossier_{schema,mapper,dependencies}.py` + endpoint `POST /api/manual-input` (Pydantic v2, FastAPI Annotated Depends, RuleRegistry через `lru_cache`). End-to-end: payload → Borrower+ManualChunk → use case → 17 правил → ScoringService → JSON. 10 integration-тестов покрывают 4 правила + 422-валидацию. 264 passed; ruff + mypy --strict зелёные. |
| 2026-05-08 | 2 | **Session close** (2.0 → 2.4.1) | За одну сессию закрыто 4 атомарных шага Phase 2 на ветке `feat/phase-2-data-adapters` (`b4c0d3f` → `0ee4cf3` → `4127a3d` → `7408aae`). End-to-end pipeline работает: payload → snapshot → 17 правил → score → JSON. Реальный CSV папы (25 292 invoices, 2020–2026, 111 контрагентов) парсится без ошибок. Открытое: 2.4.2 UI — следующая сессия. |
| 2026-05-08 | 2 | 2.4.2 ManualInput UI (3 шага) | По дизайну Claude Design (`Credit Assessment - Step 1/2/3.html`): дарк-сайдбар CreditScope, topbar с крошками, page-head + дело-чип, 3-шаговый stepper, info-banner per step, форма с rh-form + zod. Step 1 — 8 полей борровера; Step 2 — 2 квартальные таблицы 3×4 + 4 годовых поля + computed D/A & equity; Step 3 — loan amount/term/rate/purpose + DSCR pre-score panel + checklist. Submit через TanStack Query → `DossierResult` (score, recommendation, severity breakdown, red flags). Дизайн-токены в globals.css, шрифты Inter + JetBrains Mono. tsc + lint + `next build` + smoke (curl /manual-input + curl /api/manual-input) зелёные. Открытые TODO[CA-003..005]. |
| 2026-05-08 | 2 | **Session close** (2.4.2) | Frontend-сессия. 24 новых файла + 5 модифицированных, коммит `0e62486` запушен в origin. End-to-end проверен: dev server отдаёт `/manual-input`, реальный POST к `/api/manual-input` возвращает корректный `DossierResponse` с red_flags. Открытое: 2.5 persistence — следующая сессия. |
| 2026-05-08 | 2 | 2.5.1 Alembic + async engine | Зависимости: `sqlalchemy[asyncio]`, `asyncpg`, `alembic`, `testcontainers[postgres]` (dev). `Settings.database_url` + `draft_ttl_days`. `database.py` — `Base`, lazy async engine + session factory, `get_session()` FastAPI dep. `alembic.ini` (script_location в `src/infrastructure/persistence/migrations`, ruff post-write hooks, URL из Settings). `env.py` async через `async_engine_from_config` + `run_sync`, `WindowsSelectorEventLoopPolicy` для Windows. Compose-postgres переведён на host **5433** (локальный Postgres держал 5432 → asyncpg ловил RST). Smoke: `alembic current` zelёный, ruff + mypy + 264 теста зелёные. |
| 2026-05-08 | 2 | **Session close** (2.5.1) | Persistence-foundation сессия. 11 файлов в коммите `cf1c409` (5 модифицированных + 6 новых, +701/−5 строк). Smoke `alembic current` против compose-postgres на 5433 — зелёный. **Push в origin не прошёл** из-за сетевого сбоя (`github.com:443` недоступен, ping 100% loss) — коммит локальный. Открытое: 2.5.2 ORM-модели (Borrower/Snapshot/Dossier/Draft) + push при возврате сети — следующая сессия. |
| 2026-05-08 | 2 | 2.5.2 ORM models | `models/{borrower,borrower_snapshot,dossier,draft}.py` (5 файлов, +205/−3). BorrowerORM — нормализованная (UUID PK, unique INN, audit). Snapshot — JSONB payload (immutable артефакт), FK borrower с RESTRICT. Dossier — нормализованные `score`/`recommendation`/`rules_version` + JSONB `red_flags`/`severity_breakdown`. Draft — JSONB payload, `expires_at`, no owner. `env.py` импортирует пакет моделей для autogenerate. Smoke: 4 таблицы в `Base.metadata`, ruff + mypy + 264 теста зелёные. Коммит `5ed4151` запушен. |
| 2026-05-08 | 2 | 2.5.3 initial migration | `alembic revision --autogenerate` → `1e51c05eab8c_initial_schema.py` (+144/−4). Все 4 таблицы, FK ON DELETE RESTRICT, JSONB-колонки, TIMESTAMPTZ с server_default `now()`, 4 индекса. Smoke: `upgrade head` → `downgrade base` → `upgrade head`, схема симметрична. `alembic.ini` post_write_hooks переведены на `exec`-тип (надёжнее под uv). `tzdata` запинен (без него Alembic падал на Windows tz-lookup). Коммит `b9b4fff` запушен. TODO[CA-006]: дубль `ix_borrowers_inn` рядом с unique. |
| 2026-05-08 | 2 | 2.5.4 repos + mappers | 17 файлов / +905 строк. 4 Protocol-порта, DTO `DossierRecord`, 3 mappers (borrower поле-в-поле, snapshot — детерминированный JSONB с Decimal→str/date→ISO, dossier — RedFlag↔JSONB), 4 SQLAlchemy-репозитория (Borrower с `ON CONFLICT (inn) DO UPDATE` UPSERT, Snapshot insert+join-чтение, Dossier insert/get, Draft CRUD с TTL и purge). 8 round-trip mapper-тестов без БД. ruff + mypy --strict + 272 теста зелёные. Коммит `fd45abf` запушен. |
| 2026-05-08 | 2 | 2.5.5a refactor LoanRequest VO + CA-005 | Новый VO `LoanRequest(amount, term_months, rate_pct, purpose, category)` заменил одиночный `loan_request_amount` end-to-end: domain entity, правило `LOAN_TO_REVENUE_RATIO`, ManualChunk, build_borrower_snapshot, snapshot mapper (JSONB), pydantic schema `LoanRequestInput`, dossier_mapper, frontend `_form-mapper.ts` (rate "18,5"→"18.5"). CA-005 закрыт: term/rate/purpose/category из UI Step 3 теперь идут в payload. 17 файлов / +261/−36, 4 новых VO-теста, всего 276. Коммит `ce49b93` запушен. |
| 2026-05-08 | 2 | 2.5.5b wire endpoint persistence | `DossierStorage` (4 репо одной DI-зависимостью, тесты оверрайдят на in-memory dummies). `get_session` обёрнут в `session.begin()` — auto commit/rollback. `engine.json_serializer` обрабатывает Decimal→str (rule evidence). Endpoint async, цепочка upsert/save/save в одной транзакции. `DossierResponse.dossier_id: UUID`. `interfaces/api/main.py` ставит `WindowsSelectorEventLoopPolicy` при импорте. Smoke: `curl POST /api/manual-input` → 200 с `dossier_id`, в БД 1+1+1 строк. 7 файлов / +194/−8. Коммит `1301525` запушен. |
| 2026-05-08 | 2 | 2.5.6 drafts API | Три endpoint'а под `/api/manual-input/draft`: POST (201+id+expires_at), PUT (update + продление TTL, 404), GET (payload + 404). Payload — `dict[str,Any]` без strict-валидации; final-валидация только на финальном `POST /api/manual-input`. `StorageDep` промоутнут в `dossier_storage.py` для шаринга между роутерами. 7 новых тестов через stateful in-memory `_StatefulDraftRepo`. Smoke: curl create→get→put→get-after-put→404 всё зелёное; в БД payload обновлён, expires_at = created_at + 30d. 6 файлов / +226/−2, всего 283. Коммит `783fc13` запушен. |
| 2026-05-08 | 2 | **Session close** (2.5.1 → 2.5.6) | Большая persistence-сессия: 7 атомарных шагов за раз. Коммиты в `feat/phase-2-data-adapters`: `cf1c409` (Alembic+engine) → `5ed4151` (ORM) → `b9b4fff` (initial migration) → `fd45abf` (repos+mappers) → `ce49b93` (LoanRequest VO + CA-005 e2e) → `1301525` (wire endpoint) → `783fc13` (drafts API), плюс 4 docs-коммита. Всего ~+2400 строк. ruff + mypy --strict + 283 теста зелёные на каждом шаге. Postgres compose на host port 5433; миграция `1e51c05eab8c` применена; end-to-end smoke двух API (manual-input + draft) против реальной БД пройден. CA-005 закрыт. Открытое: 2.5.6.b frontend draft integration — следующая сессия. |
| 2026-05-08 | 2 | 2.5.6.b frontend draft integration | `_hooks/use-form-draft.ts` — TanStack `useMutation`, POST/PUT с PUT-404→POST fallback, save на переходе step (fire-and-forget). Mount: `?draft={id}` → GET → `form.reset(payload)`; 404 / битый id → query-param чистится `window.history.replaceState` (без `router.replace` чтобы не триггерить server-roundtrip). Topbar prop `draft: DraftIndicator` (saving / saved / error) → бейдж «Черновик сохранён HH:MM». Page обёрнута в `Suspense` — Next App Router требует boundary вокруг `useSearchParams` при prerender. `lib/api.ts` расширен `createDraft / updateDraft / getDraft` + общий `jsonFetch`. 4 файла / +314/−16. eslint + tsc + `next build` зелёные; smoke API: `POST 201 → GET 200 → PUT 200 → GET-after-put 200 → битый-id PUT 404`. UI-логика проверена сборкой + HTTP 200 на 3 URL; ручной браузерный smoke (form.reset / replaceState / индикатор) остался за пользователем. Коммит `b9f1468` запушен. |
| 2026-05-08 | 2 | 2.5.6.b regression — playwright smoke | Playwright headless Chromium прогнал 3 сценария через ad-hoc temp-окружение: `?draft=` появляется после goNext + индикатор «Черновик сохранён HH:MM», reload по id наполняет форму через `form.reset()`, битый UUID чистится из URL. POST/GET network calls подтверждены через `page.on("request")`. Pre-existing hydration mismatch на `Math.random()` в `caseId` найден в логах browser pageerror — выписан как TODO[CA-007]. |
| 2026-05-08 | 2 | CA-007 — hydration mismatch на caseId | `Math.random()` в `useMemo` в page.tsx → разный SSR vs CSR id. Заменён на `useSyncExternalStore` с null-снапшотом сервера и client-only кэшем (модульный singleton — id переживает rerender, не shared между tab'ами). PageHead принимает `string \| null`, `—` до mount. Регрес-smoke в headless Chromium: pageerror'ы пропали, draft flow цел. 2 файла / +27/−6. Коммит `18eeb74` запушен. |
| 2026-05-08 | 2 | 2.5.7 testcontainers integration tests | `tests/integration/conftest.py`: session-scope `PostgresContainer("postgres:16-alpine")`, `alembic upgrade head` через DATABASE_URL env-var + `get_settings.cache_clear`, function-scope `pg_engine` (pytest-asyncio закрывает loop после каждого теста — session-scope engine ловил «Event loop is closed»), `pg_session` на outer-tx + savepoint (`join_transaction_mode="create_savepoint"`). Авто-skip без Docker. 17 новых тестов: borrower (UPSERT insert/update path), snapshot+dossier (JSONB Decimal/date, FK RESTRICT), draft (CRUD/TTL/purge), api/manual_input_e2e (POST → 1+1+1 строк в БД, draft full cycle с 404). **Bug в попутку:** `_json_default` в `database.py` не умел date/datetime — любой rule с date в evidence ронял save в JSONB. Добавлено `isinstance(obj, datetime \| date) → isoformat()`. Marker `integration` в pyproject; README документирует `pytest -m "not integration"` (1.35s) и `pytest -m integration` (~5s warm, ~10s cold). 12 файлов / +631/−4. ruff + mypy --strict + 300 тестов зелёные. Коммит `a60c87f` запушен. |
| 2026-05-08 | 2 | 2.5.8 ADR 0005 + Phase 2.5 DoD | `docs/adr/0005-drafts-auth-by-uuid.md`: фиксирует модель auth черновиков формы для Phase 2 — UUID4 identifier, без owner-поля, TTL=30d, payload без strict-валидации (final POST валидирует). Таблично угрозы + mitigation. Phase 4 plan — добавить `owner_user_id` при Bank SSO, UUID-only остаётся fallback для Accountant Mode. DoD-чеклист подфазы 2.5: 12 пунктов ✅, open items (CA-003/004/006 + scheduled purge) перечислены явно — не блокируют закрытие. Только docs, runtime не затронут, 300 тестов прежние. Коммит `bf4d019`. |
| 2026-05-08 | 2 | **Phase 2.5 → main** (PR #1 merged) | PR #1 «Phase 2.5 — Persistence layer» смержен в `main` (merge commit `5400f9e`). На main приехали 27 коммитов / +8611/−138 за период 2.0 → 2.5.8: domain refactor под реальный CSV (ADR 0004), use case + EsfCsvAdapter, manual-input API, manual-input UI 3 шага, async SQLAlchemy + Alembic + 4 ORM/repos/mappers, draft endpoints + frontend integration, testcontainers integration tests с 17 тестами, два bug-find'а (CA-005 LoanRequest VO end-to-end, _json_default не умел date/datetime, CA-007 hydration mismatch на caseId), ADR 0005. Ветка `feat/phase-2-data-adapters` локально удалена; remote живёт до решения пользователя. |
| 2026-05-08 | 2 | 2.3 Day 1 — SoliqXltxAdapter parsers | Папа прислал 5 xltx-форм за свою фирму (декларация НДС март 2026 + ilova-реестр ЭСФ построчно с НДС + Form №2 Q4 2025 + Form №1 Q4 2025 + Расчёт прибыли Q1 2026). Discovery показало: гипотеза «SoliqExcelAdapter = выгрузка оборотов» из бриф/CLAUDE-доки была неверной — my3.soliq.uz отдаёт **готовые формы отчётности** в xltx. Track A (закрыть `VAT_ESF_MISMATCH`) выбран user-ом. Ветка `feat/soliq-xltx-vat-adapter` от main, scope = парсеры + facade + smoke (без domain-интеграции). Реализованы `infrastructure/adapters/soliq_xltx/`: `format_detector.py` (5 типов по сигнатурным cells list01), `header_parser.py` (общая шапка для VAT/Form 1/2/Profit Tax), `vat_declaration_parser.py` (list02 R6-R11 sales по каналам ЭСФ/ККМ/экспорт/MP/прочие, list04 НДС к зачёту, поддержка 'x'-маркеров), `vat_registry_parser.py` (list01 закупки + list02 продажи, data rows R15+, Decimal-точные totals, ИНН nullable для ПИНФЛ-розницы), `parser.py` facade с диспетчем и поддержкой Path/bytes/BinaryIO. Synthetic factories `tests/fixtures/soliq_xltx/_factories.py` — конструируют openpyxl Workbook in-memory без xltx-бинарников в git. Real-data smoke (~/Downloads, ИНН 306399449): declared_vat = 62 799 985.69 ↔ ilova sum_sales_vat = 63 550 200.23 → diff 1.19% (<15%, правило молчит, фирма здоровая). Числа sales/purchases между декларацией и реестром сходятся до копейки. 17 файлов / +1617/−1, 51 unit-тест, 334 passed; ruff + mypy --strict зелёные. `openpyxl` в runtime deps, `types-openpyxl` в dev. `tests` добавлено в `known-first-party` (isort). `.gitignore`: `tests/fixtures/**/*_full.xltx` + `scripts/_smoke_*.py`. Коммит `dab56d4` запушен. |
| 2026-05-08 | 2 | **Session close** (2.3 Day 1) | Discovery + plan + implement за одну сессию. На ветке `feat/soliq-xltx-vat-adapter` два коммита: `dab56d4` (feat — adapter package + 51 тест) и `158b353` (docs — CLAUDE.md sync), оба запушены в origin. Real-data smoke прогнан против папиных файлов в `~/Downloads`, цифры подтверждены. Открытое: 2.3 Day 2 (domain integration) — следующая сессия. Дизайн-вопрос Day 2: где хранить месячный VAT-период (новое поле `monthly_vat_reports` или допущение в `quarterly_reports`) и как обновить правило `VAT_ESF_MISMATCH` (latest period с обоими полями `vat_declared` + `esf_seller_vat_total`, не строго годовой). Day 3: API endpoint `POST /api/upload/soliq-xltx` (multipart) + UI Step 2 «Загрузить из Soliq» + ADR 0006 + financе CLAUDE.md update перед PR на main. |
| 2026-05-08 | 2 | **Phase 2.3 → main** (PR #2 merged) | PR #2 «Phase 2.3 — SoliqXltxAdapter (parsers + domain + API + UI)» смержен в `main` (merge commit `6647a2b`). На main приехали 7 коммитов / +3215/−72 за период Day 1 + Day 2 + Day 3: парсеры xltx-форм Soliq (5 типов через `format_detector`, специализированные парсеры для VAT_DECLARATION + VAT_REGISTRY_ILOVA), domain-сущность `VatPeriodReport` с переписанным правилом `VAT_ESF_MISMATCH` под месячные периоды (ADR 0006), маппер xltx → SoliqChunk с `period_month` параметром, persistence JSONB-payload обновлён под `vat_periods`, API endpoint `POST /api/upload/soliq-xltx` (multipart auto-detect, локализованные 422), UI компонент `SoliqUpload` на Step 2 (file slots + year/month picker + preview). Real-data smoke на файлах папы прошёл; ruff + mypy + 365 тестов + frontend build зелёные. Локальная ветка `feat/soliq-xltx-vat-adapter` устарела, remote живёт. |
| 2026-05-08 | 2 | 2.3 Day 3 — API endpoint + UI Step 2 | Backend: `interfaces/api/shared/soliq_upload.py` (NEW) — endpoint `POST /api/upload/soliq-xltx` (multipart, `files: list[UploadFile]` + `Form(borrower_inn)` + `Form(period_month, ge=1, le=12)`). Auto-detect через `detect_format`, валидация пары (1×VAT_DECLARATION + 1×VAT_REGISTRY_ILOVA, иначе 422 с локализованным сообщением). Pydantic `SoliqUploadResponse(period, vat_declared, esf_seller_vat_total, organization_name, submitted_at, diff_pct)`. Размер файла ≤5 MB → 413. Маппинг ошибок: `XltxBorrowerMismatchError` / `MalformedXltxError` / `UnsupportedFormatError` → 422 с `detail`. Зависимость `python-multipart` добавлена в pyproject. Тесты `soliq_upload_test.py` — 9 кейсов через `TestClient` + factory in-memory bytes (success в любом порядке, две декларации/ilovы, один файл, неверный формат через Form 2, INN-mismatch, period_month=13, плохой ИНН). Frontend: `web/src/lib/api.ts` — `uploadSoliqXltx({ files, borrowerInn, periodMonth })` через `FormData`; `_schema.ts` — `step2.vatPeriod: z.object({ year, month, vatDeclared, esfSellerVat, organizationName?, submittedAt?, diffPct? }).nullable()`; `_form-mapper.ts` — если `vatPeriod` заполнен → `payload.vat_periods = [{ period: {start, end}, vat_declared, esf_seller_vat_total, submitted_at? }]`; новый компонент `_components/soliq-upload.tsx` (Controller над `step2.vatPeriod` + два file slots + year/month select + кнопка «Распознать» disabled до 2 файлов и валидного INN из Step 1; preview-блок с тремя статами; диф-токен зелёный/жёлтый по 15%-порогу; кнопка «Сбросить»). Step 2 теперь начинается с `<SoliqUpload />` поверх квартальных таблиц. Real-data smoke: uvicorn → curl с парой файлов папы (`~/Downloads/...M_3_*`) → 200 с `period 2026-03-01..03-31`, `vat_declared 62799985.69`, `esf_seller_vat_total 63550200.23`, `diff_pct 1.19%`; негативные сценарии (wrong INN, two declarations, period_month=13) — 422. 8 файлов / +617/−16: 3 NEW (`soliq_upload.py`, `soliq_upload_test.py`, `soliq-upload.tsx`) + 5 modified (`app.py`, `pyproject.toml`/`uv.lock`, `step-2-financials.tsx`, `_form-mapper.ts`, `_schema.ts`, `lib/api.ts`). ruff + mypy --strict + 365 тестов (с 356 в Day 2) + tsc + lint + next build зелёные. Browser-smoke (ручная загрузка через UI) остался на пользователя. |
| 2026-05-08 | 2 | 2.3 Day 2 — monthly VAT periods + xltx mapper | Дизайн-решения подтверждены пользователем: (1) удалить старое одиночное `BorrowerSnapshot.esf_seller_vat_total`, (2) `period_month` — явный параметр в маппере, не из workbook. Введена сущность `VatPeriodReport(period: DateRange, vat_declared, esf_seller_vat_total, submitted_at)` — заменяет одиночный агрегат списком `vat_periods` в `BorrowerSnapshot`. Правило `VAT_ESF_MISMATCH` переписано: latest period с обоими полями → diff>15% → fires; degraded silent без полного периода. `SoliqChunk` + `ManualChunk` получили `vat_periods`; use case `build_borrower_snapshot` extend'ит. `ManualInputRequest` (Pydantic) теряет `esf_seller_vat_total: MoneyInput \| None`, получает `vat_periods: list[VatPeriodInput]` (orphan-поле было — frontend его не отправлял). Persistence JSONB-payload: массив `vat_periods` вместо одиночного поля; `_vat_period_to/from_dict` round-trip. Маппер `infrastructure/adapters/soliq_xltx/snapshot_mapper.py:to_soliq_chunk(declaration, registry, *, borrower_inn, period_month)` собирает один VAT-период из пары: vat_declared←declaration.vat_charged_total, esf_seller_vat_total←registry.sales_vat_total, period вычисляется через `calendar.monthrange`, ИНН-mismatch → `XltxBorrowerMismatchError`. Synthetic fixture `critical_borrower` обновлён под новый формат. ADR 0006 (новый) фиксирует дизайн; ADR 0004 помечен «partially superseded by 0006» в шапке + note-блок. E2E smoke против реальных файлов папы (март 2026, ИНН 306399449) в `scripts/_smoke_day2_xltx.py` (gitignored): case A — diff 1.19% < 15% → правило молчит; case B — синтетическая подмена `esf_seller_vat_total = declared/2` → fires на 50%. 18 файлов / +720/−64; 5 новых (`vat_period_report.py` + test, `snapshot_mapper.py` + test, ADR 0006). ruff + mypy --strict + 356 тестов (вместо 334 в Day 1) зелёные. Коммит `4e6f47d` запушен. |
| 2026-05-09 | 3 | 3.A Result Screen UI (10 stages) | Frontend-first: страница `/(accountant)/dossier/[id]/page.tsx` по дизайну `пдф.png` (banking dashboard). Decomposition 10 stages за одну сессию: (1) recharts + shadcn add card+badge + skeleton route; (2) `_components/sub-header.tsx` (BR-XXX badge + status pill + actions «Документы»/«Карточка клиента») + `action-bar.tsx` (PDF disabled + Назад на /manual-input); (3) `score-gauge.tsx` — inline SVG полудуга с 4 цветными секторами (red→orange→yellow→green) и стрелка `transform: rotate((score-50)*1.8°)`, helper `arcPath(cx,cy,r,startDeg,endDeg)`; (4) `borrower-card.tsx` — Card с info-table (name/ИНН/ОПФ/регистрация/директор/ОКВЭД/адрес) + «Проверено в ГНК» pill; (5) `kpi-card.tsx` (label + big mono value + ▲/▼ change pill + Recharts AreaChart sparkline 60×24, gradient stroke по changeTone) + `kpi-row.tsx` (4 KPI: revenue_ltm/ebitda/roe/debt_to_ebitda) + `_lib/format.ts` (`formatBigUzs`/`formatPct`/`formatRatio`/`formatYoy`/`formatMonthShort`); (6) `revenue-24m-chart.tsx` — Recharts ComposedChart, Bar по месяцам с `<Cell fill={isPeak?orange:gray}>`, Line trend 12-мес rolling, period select 6/12/24 (useState), легенда снизу; (7) `risk-signals.tsx` — компактный список (severity dot + русское название из RULE_LABEL по rule_id + evidence-pill `renderEvidenceValue` по rule_id, `pluralFlags`); (8) compose page — Server Component с `await params.id` → `mockDossier(id)`, layout `[Topbar]→SubHeader→grid 320px/1fr (Gauge|Borrower)→KpiRow→Revenue chart→RiskSignals→ActionBar`; (9) wire from `manual-input/page.tsx`: убран `result` state и inline `<DossierResult>`, `submitMutation.onSuccess` → `router.push(/dossier/${data.dossier_id})`; добавлен `dossier_id: string` в `lib/api.ts:DossierResponseDto`; (10) **CA-004 закрыт** — Step 2 формы получил per-year `taxesPaid23/24/25` (вместо одного `taxesPaid`), `_form-mapper.ts` кладёт каждое значение в свой annual_report. **Mock data shape** `DossierViewDto extends DossierResponseDto` спроектирован под будущий backend GET endpoint (Phase 3.B), `mockDossier(id)` строит данные ООО «Полярная Звезда» (score 73, 4 KPI, 24-мес revenue с сезонными пиками в декабре, 4 risk signals). 20 файлов / +1560/−53. Tooltip-formatter переписан под `recharts` Formatter type. `tsc --noEmit` + eslint + `next build` зелёные; smoke `curl /dossier/test-uuid HTTP 200` + content-grep по русским заголовкам. SSR-warning Recharts `width(-1)` — безвредный, hydration рисует. Коммит `4f40515` запушен на ветку `feat/phase-3a-result-screen`. |
| 2026-05-09 | 3 | 3.A polish — soft warm gray bg + off-white cards | По запросу пользователя: контент-панель читалась как «слепящее белое» рядом с тёмным sidebar. Снижаем контраст в `globals.css`: `--ca-bg #F6F7F9 → #F1F5F9` (заметнее серый), `--ca-surface #FFFFFF → #FCFCFD` (off-white), `--background` и `--card`/`--popover` синхронизированы. Сделан `replace_all` `bg-white` → `bg-[var(--ca-surface)]` по 19 .tsx (10 dossier + 8 manual-input + topbar) — единая поверхность через токен, без захардкоженного `#FFFFFF`. Subtle `--ca-border #E4E7EC` остался — карточка визуально отделена от фона. Scope ограничен `(accountant)` layout, главная и shadcn ui/* (button/input) не тронуты. tsc + eslint + smoke 200 — зелёные. Коммит `811e964` запушен. |
| 2026-05-09 | 3 | 3.A fix — native HTML5 date picker | Дата регистрации и дата назначения директора в Step 1 формы были текстовыми инпутами с маской дд.мм.гггг + декоративная иконка `<Calendar />` без всплывающего picker'а — пользователь пожаловался что нет календарного попапа. Перевод на нативный `<input type="date">`: `_schema.ts` `ddMmYyyy → isoDate` (regex `^\d{4}-\d{2}-\d{2}$`), `parseRu → parseIso`, refine на cross-field на ISO; `_form-mapper.ts` убран `toIsoDate` — value уже ISO; `step-1-borrower.tsx` `<FieldInput type="date" min={regDate} max={today}>` (HTML-уровень валидации «назначение ≥ регистрации» + zod refine на submit), убран `InputGroup` с иконкой Calendar, `derivedActivityHint` парсит ISO, добавлен helper `todayIso()`. Display-форматирование `dd.MM.yyyy` в `dscr-summary.tsx` и `step-3-loan.tsx` не тронуто (это вывод, не ввод). 3 файла / +40/−38. Коммит `73caf31` запушен. |
| 2026-05-09 | 3 | **Session close** (Phase 3.A) + PR opened | Frontend-сессия 10 stages + 2 polish-фикса + docs-коммит. На ветке `feat/phase-3a-result-screen` 4 коммита: `4f40515` (feat — result screen UI), `811e964` (style — soft warm gray bg), `73caf31` (fix — native date picker), `582ce24` (docs — Phase 3.A closed). Всего 32 файла / +1646/−129. End-to-end smoke прогнан против real Postgres-5433 + uvicorn + `next dev`: `POST /api/manual-input` → HTTP 200 + `dossier_id`, `GET /dossier/<любой_id>` → 200 (mock-данные), redirect c `/manual-input` Step 3 submit на `/dossier/<реальный_uuid>` работает. CA-004 закрыт по пути. **Решение пользователя: 3.A и 3.B мержатся раздельными PR.** PR через web UI: https://github.com/RiobVO/credit-assistant/pull/new/feat/phase-3a-result-screen (gh CLI требует интерактивной auth — отдано пользователю). Открытое для Phase 3.B: маппер `domain risk_score (lower=better) ↔ display_score (higher=better)` — сейчас mock в banking-шкале 73/100, надо договориться (инверсия `100 - score`?). |
| 2026-05-09 | 3 | **Phase 3.A → main** (PR #3 merged) | PR #3 «Phase 3.A — Dossier Result Screen UI» смержен в `main` (merge commit `5f3c5b8`). На main приехали 5 коммитов / 33 файла / +1649/−132 за период Phase 3.A: новая страница `/(accountant)/dossier/[id]` (banking dashboard на Recharts + shadcn/ui по дизайну `пдф.png`), redirect от `manual-input` Step 3 submit на `/dossier/<dossier_id>`, mock-данные через `mockDossier(id)` (заменятся в Phase 3.B), CSS-токены смягчены (off-white карточки), нативный HTML5 date picker для Step 1 дат, CA-004 закрыт (per-year taxes UI). Локальная ветка `feat/phase-3a-result-screen` удалена; origin-ветка живёт до решения пользователя. Phase 3.B стартует на новой ветке от main. |
| 2026-05-10 | 3 | 3.B.1 — DossierViewRecord + get_view_by_id | Ветка `feat/phase-3b-dossier-view-api` от main HEAD `5771560`. `application/dto/dossier_view_record.py` — read-DTO `DossierViewRecord(dossier_id, dossier, snapshot, created_at)`; borrower не дублируется — берётся через `view.snapshot.borrower`. `SqlAlchemyDossierRepository.get_view_by_id(id)` — single SELECT с двумя JOIN: `dossiers → borrower_snapshots → borrowers`. 2 integration теста (round-trip happy path + 404) против real Postgres через testcontainers. ruff + mypy --strict + 365 unit + 19 integration зелёные. Коммит `36671ef` запушен. |
| 2026-05-10 | 3 | 3.B.2 — KPI calculator + LoadDossierForView | `application/dto/kpi_bundle.py` (`KpiValue/KpiBundle/KpiUnit/MonthlyRevenuePoint`), `application/services/kpi_calculator.py:compute_kpis` (revenue_ltm: priority monthly_turnover ≥12 → LTM + sparkline + YoY с prev 12; fallback annual; иначе None; UZS-only) + `compute_monthly_revenue_24m` (последние 24 точки с 12-мес rolling trend и top-3 peak detection); EBITDA/ROE/Debt-to-EBITDA всегда None — компонентов нет в snapshot, не выдумываем. `application/ports/dossier_view_repository_port.py` (read-only Protocol). `application/use_cases/load_dossier_for_view.py` — `LoadDossierForView` orchestrator + `DossierViewBundle(view, kpis, monthly_revenue_24m)`. 13 unit-тестов calculator (3 источника + degraded + chart + filters) + 2 use case теста с fake-репо. ruff + mypy --strict + 380 unit зелёные. Коммит `a54fe5d` запушен. |
| 2026-05-10 | 3 | 3.B.3 — GET /api/dossier/{id} endpoint | Pydantic-схемы `DossierViewResponse` + `BorrowerOutput` + `ApplicationOutput` + `KpiValueOutput`/`KpiBundleOutput` + `MonthlyRevenuePointOutput`. `RiskScoreOutput.display_score: int = 100 − score` (правило A) — clamped 0..100; присутствует и в POST, и в GET. Decimal сериализуется как str (защита от потери точности на UZS-млрд). `_application_id(dossier_id, created_at) → "BR-{year}-{XXXX}"` (uppercase first 4 hex), status пока всегда `in_review`. Endpoint `@router.get("/dossier/{dossier_id}")` рядом с POST: 404 на None, 200 DossierViewResponse. `DossierRepositoryPort(DossierViewRepositoryPort, Protocol)` — наследование Protocol'ов; stub-репы в `dossier_test.py` / `draft_test.py` получили `get_view_by_id` для соответствия. 4 integration-теста против real Postgres: POST→GET happy path с проверкой формы (display_score = 100−score, application id `BR-…`, kpis.revenue_ltm посчитан, 12 точек чарта в хронологическом порядке) / degraded без monthly / 404 / 422 на битый UUID. ruff + mypy --strict + 380 unit + 23 integration зелёные. Коммит `727ae22` запушен. |
| 2026-05-10 | 3 | 3.B.4 — frontend useQuery + skeleton + dead code cleanup | `lib/api.ts`: `getDossier(id)` + `DossierViewDto` зеркалит backend (Decimal как str, KPI и yoy_pct nullable, display_score в RiskScoreDto). `dossier/[id]/page.tsx` → server component тонкий (await params + render `DossierView`); `_components/dossier-view.tsx` (NEW) — `"use client"` + TanStack `useQuery({queryKey:["dossier",id]})` + retry: 1, ScoreGauge получает `display_score`. `_components/dossier-skeleton.tsx` (NEW) — animate-pulse blocks повторяют layout (gauge + borrower + 4 KPI + chart + signals). `_components/dossier-error.tsx` (NEW) — 404 «Досье не найдено» + 500 «Не удалось загрузить» с retry-кнопкой и ссылкой «К форме». `kpi-row.tsx` → KpiSlot с null-fallback (`EmptyKpiCard` рисует «—» + «Нет данных для расчёта»), parseFloat для string-значений, `up_is_good` / `down_is_good` для тона. `kpi-card.tsx` → yoyPct nullable (если null — pill не отрисовывается). `revenue-24m-chart.tsx` → парсинг str→number один раз в начале + EmptyChart-fallback при пустом массиве (CTA «Загрузите Soliq на Шаге 2»). `borrower-card.tsx` → расширена legal_form (6 значений: llc/pe/ltd/jsc/ie/other). **Удалены:** `_lib/mock-dossier.ts` (CA-009 закрыт) + `manual-input/_components/dossier-result.tsx` (CA-008 закрыт) — grep по проекту показал что нигде не импортируются. tsc + eslint + `next build` зелёные; **browser-smoke в headless Chromium через Playwright (temp env)**: `POST /api/manual-input` → `dossier_id=b169c7e8-…` → `GET /api/dossier/{id}` → 200 → hydration без pageerror'ов; видны badge `BR-2026-B169`, gauge со score 100, KPI Revenue LTM посчитан + 3 пустых слота, чарт «Выручка — 24 мес.». 11 файлов / +381/−489 (за счёт удаления dossier-result.tsx + mock-dossier.ts). Коммит `ded825f` запушен. |
| 2026-05-10 | 3 | **Session close** (Phase 3.B + ADR 0007) | Backend+frontend сессия 5 стадий за раз. На `feat/phase-3b-dossier-view-api` 4 коммита: `36671ef` (3.B.1 read-модель + JOIN) → `a54fe5d` (3.B.2 KPI calculator + use case) → `727ae22` (3.B.3 endpoint + Pydantic + integration tests) → `ded825f` (3.B.4 frontend useQuery + skeleton + dead code), плюс docs-коммит закрытия. Всего ~+1600 строк кода + 6 новых интеграционных тестов. ADR 0007 фиксирует score mapping policy (правило A). Финальный verify: ruff + mypy --strict + **403 теста** (380 unit + 23 integration) + tsc + eslint + next build + browser-smoke с реальным backend → всё зелёное. CA-008 + CA-009 закрыты. Phase 3.B готова к PR #4 на main. **Открытое:** Phase 3.C (PDF endpoint через WeasyPrint+Jinja2+matplotlib) — стартует после merge PR #4. |
| 2026-05-10 | 3 | **Phase 3.B → main** (PR #4 merged) | PR #4 «Phase 3.B — GET /api/dossier/{id} + KPI calculator + frontend useQuery» смержен в `main` (merge commit `317884f`). На main приехали 5 коммитов за период Phase 3.B: read-модель `DossierViewRecord` + `get_view_by_id` single SELECT с двумя JOIN (3.B.1), KPI-калькулятор + `LoadDossierForView` (3.B.2), endpoint `GET /api/dossier/{id}` + Pydantic-схемы + display_score правило A (3.B.3), frontend useQuery + skeleton + error UI + dead-code cleanup CA-008/CA-009 (3.B.4), ADR 0007 + closure (3.B.5). Локальная и origin ветки `feat/phase-3b-dossier-view-api` удалены через `gh pr merge --delete-branch`. **gh CLI авторизован** (RiobVO via keyring, scopes `repo,workflow,gist,read:org`) — следующие PR можно открывать/мержить из CLI без web UI round-trip. Phase 3.C стартует на новой ветке `feat/phase-3c-...` от `main` HEAD `317884f`. |
| 2026-05-10 | 3 | 3.C.0 — Dockerfile + compose api service | Дизайн-решение: WeasyPrint требует Pango/HarfBuzz/Fontconfig нативно, на Windows-хосте без GTK runtime импорт падает (OSError на libgobject). Probe install подтвердил: pip-уровня хватит на Linux, не Windows. Пользователь выбрал подход «весь backend в Docker». `Dockerfile` (python:3.12-slim + apt libpango-1.0-0/libpangoft2-1.0-0/libharfbuzz0b/libfontconfig1 + uv 0.5.11 binary + COPY src+config+alembic.ini + entrypoint.sh с `alembic upgrade head` → `uvicorn 0.0.0.0:8000`); `.dockerignore` (исключает .venv/.git/web/node_modules/секреты/dev-utils); compose service `api` (build context `.`, depends_on postgres healthy, DATABASE_URL→`postgres:5432`, CORS_ALLOWED_ORIGINS как JSON-массив для pydantic-settings). Healthcheck через curl на /health. **Probe smoke в контейнере:** `weasyprint.HTML(string=...).write_pdf()` → %PDF-1.7, 7299 bytes, кириллица через DejaVu Sans читается. Локально `ruff + mypy --strict + 380 unit` зелёные (новые deps `weasyprint>=63 + jinja2>=3.1 + matplotlib>=3.9` не сломали). Коммит `2d8fc87` запушен. |
| 2026-05-10 | 3 | 3.C.1 — application слой (PdfReportPort + bundle + use case) | `application/dto/dossier_pdf_bundle.py` — `DossierPdfBundle` (композирует `DossierViewBundle` + top-N контрагенты + tax_summary), `CounterpartyShare` (с `is_new = months_since_registration < 6`), `TaxDelayEntry`, `TaxSummary`. `application/services/pdf_data_aggregator.py` — чистые функции `compute_top_buyers` / `compute_top_suppliers` (сортировка по share desc, truncate до limit, фильтр через directory) и `compute_tax_summary` (просрочки + штрафы окно 3y, freezes окно 12m — границы согласованы с правилами TAX_PAYMENT_DELAYS и BANK_ACCOUNT_FROZEN_12M). `application/ports/pdf_report_port.py` — sync Protocol `render(bundle) -> bytes`. `application/use_cases/render_dossier_pdf.py` — `RenderDossierPdf` orchestrator: LoadDossierForView → enrich → port.render через `asyncio.to_thread`. 11 unit-тестов: 9 на aggregator (top-N сортировка/truncate, skip-without-directory, new-marker, supplier-share, tax delays окно/sort, penalties aggregation, freezes окно, empty state) + 2 на use case (None при missing dossier, передача обогащённого bundle). 391 unit (было 380), ruff + mypy --strict зелёные. Коммит `c5f680f` запушен. |
| 2026-05-10 | 3 | 3.C.2 — chart_renderer.py (matplotlib) | `infrastructure/reports/pdf/chart_renderer.py`: `render_revenue_24m(points)` — bar chart с акцентом top-3 пиков (warning amber #B8730E) + линия 12-мес rolling-тренда (primary blue 700 #1947AA), пустой series → placeholder PNG с текстом «Нет данных». `render_sparkline(series, tone=up/down)` — мини-чарт для KPI-карточки, success green / danger red по YoY-направлению, <2 точек → пустой PNG. matplotlib.use("Agg") на module-level (без GUI/Tk). 5 unit-тестов: PNG magic `\x89PNG`, размер > 1.5KB, placeholder при пустом, sparkline при <2 точках, tone=down. 396 unit (было 391), ruff + mypy --strict зелёные. Коммит `1bc276d` запушен. |
| 2026-05-10 | 3 | 3.C.3 — Jinja2-шаблон A–G + 9 фильтров | `infrastructure/reports/pdf/templates/dossier.html` — 4 страницы A4 (CSS Paged Media с `@page` running footer): header с BR-XXXX badge + status, **G** (gauge SVG inline + рекомендация), **A** (info-grid: ИНН с разрядным разделителем, ОПФ, директор, ОКВЭД, адрес), **B** (3-летняя финансовая таблица + 4 KPI-карточки с null-fallback), **C** (matplotlib-чарт через data-URI base64), **D** (двухколоночный top-N с маркером «новый <6мес»), **E** (просрочки + штрафы + freezes), **F** (red flags с severity-bar/pill/evidence). `font-family: "Inter", "DejaVu Sans"` fallback chain (Inter подключим через bundle TTF в TODO[CA-010]). `template_filters.py` — 9 фильтров с None-tolerance: `fmt_uzs` (полный + billions), `fmt_pct` (с +/− Unicode minus), `fmt_pct_share` (для CounterpartyShare 0..100), `fmt_date_ru`/`fmt_datetime_ru` (русские месяцы), `fmt_inn` (NBSP-разделитель), `severity_label`/`severity_color`/`severity_bg` (русская локализация). 24 теста: 21 фильтр + 3 smoke-render шаблона (минимальный context, пустой red_flags, missing loan_request). 420 unit (было 396), ruff + mypy --strict зелёные. **Технический момент:** Write tool иногда искажал русские строковые литералы → пришлось переписать через явные escapes и переменные `_SUM_WORD = "сум"`. Коммит `03fbf9d` запушен. |
| 2026-05-10 | 3 | 3.C.4 — WeasyPrintPdfRenderer adapter | `infrastructure/reports/pdf/pdf_renderer.py`: класс `WeasyPrintPdfRenderer` имплементирует `PdfReportPort.render`. `__init__` создаёт Jinja2 `Environment` + регистрирует все 9 фильтров. `_build_context(bundle)` маппит `DossierPdfBundle` в context dict для шаблона: application_id (`BR-{year}-{XXXX}` из uuid+year), display_score (clamped 100−score), gauge_angle_deg по той же формуле, что frontend `score-gauge.tsx` ((score-50)*1.8°), KPI слоты с null-fallback, base64 chart PNG, локализация recommendation/legal_form, flat-структура red_flags для шаблона. `render()` — lazy import `weasyprint.HTML` (на Windows-хосте без GTK падает, не должно ломать остальной код), `HTML(string=...).write_pdf()` возвращает bytes. mypy overrides: `weasyprint` без stubs (ignore_missing_imports). 2 integration-теста (pytestmark integration → skip без Docker): PDF magic `%PDF-` + размер > 5KB на полном bundle и degraded случае с пустым monthly_revenue_24m. **Smoke в credit-api контейнере** (через `docker compose exec`): полный рендер досье ООО «Тестовая фирма» → 47.5 KB %PDF-1.7, кириллица читается, gauge/KPI/чарт/секции на местах. Коммит `aff8c0b` запушен. |
| 2026-05-10 | 3 | 3.C.5 — endpoint GET /api/dossier/{id}/pdf | `interfaces/api/shared/dossier_pdf.py`: endpoint собирает `RenderDossierPdf` (LoadDossierForView + WeasyPrintPdfRenderer singleton через `lru_cache`). Lazy-import WeasyPrint оборачивается try/except OSError → **503** «PDF недоступен (нет GTK runtime)» — на Windows-хосте без GTK это понятный fallback вместо непрозрачного 500. **200**: `Content-Type: application/pdf` + `Content-Disposition: attachment; filename="BR-XXXX.pdf"` + `Cache-Control: no-store`. **404** если dossier_id не найден. **422** если UUID битый. `app.py`: include_router(dossier_pdf_router). **Bug по пути:** Dockerfile не копировал `config/` (где лежит `rules/v1_uz_msb.yaml`) → 500 на первом POST manual-input → добавлен `COPY config/`. 3 integration-теста (mark integration + skipif win32): happy path PDF + 404 + 422. **E2E smoke** в credit-api: POST → dossier_id `7d62b786-…` → GET /pdf → HTTP 200 + Content-Type application/pdf + filename="BR-7D62.pdf" + magic %PDF-1.7 + 38 436 bytes. PDF открывается, 4 страницы A–G. Коммит `ae8203d` запушен. |
| 2026-05-10 | 3 | 3.C.6 — frontend «Скачать PDF» enabled | `web/src/app/(accountant)/dossier/[id]/_components/action-bar.tsx`: ActionBar принимает `dossierId` как prop, рендерит `<a href="${API_URL}/api/dossier/${dossierId}/pdf" target="_blank" rel="noopener">` вместо disabled `<button>`. При Content-Disposition: attachment с backend браузер сам инициирует download. `dossier-view.tsx` пробрасывает dossierId в ActionBar. Старый title «Скачивание PDF будет доступно после Phase 3.C» убран. Verify: tsc --noEmit + eslint + next build зелёные. Browser-smoke не делал (понадобится next dev + Playwright; sufficient sanity = build зелёный). Коммит `15132b4` запушен. |
| 2026-05-10 | 3 | **Session close** (Phase 3.C готова к PR #5) | Большая сессия: 7 атомарных шагов 3.C.0 → 3.C.6 за один проход + ADR 0008. Ветка `feat/phase-3c-pdf-endpoint` от main HEAD `317884f`, 8 коммитов запушены. Финальный verify: ruff + mypy --strict + **420 unit + 23 integration** (5 PDF-тестов skip на Windows-хосте через `pytest.mark.skipif(sys.platform=="win32")`) + tsc + eslint + next build + e2e smoke endpoint в credit-api контейнере (POST→GET pdf 200 application/pdf %PDF-1.7) — всё зелёное. ADR 0008 фиксирует выбор стека (WeasyPrint vs ReportLab/Playwright/wkhtmltopdf), Docker-only deploy, font strategy fallback DejaVu→Inter, degraded states policy. **Открытое:** TODO[CA-010] bundle Inter+JetBrains Mono TTF — косметика, v1 PDF на DejaVu Sans читается. PR #5 готов к открытию. |
| 2026-05-10 | post-3 | **Parser best-effort refactor → main** (squash `64444d0`) | Глобальный рефактор soliq_xltx-парсера: парсер НИКОГДА не raises на данных, только на формате. **Контракт:** raise = UnsupportedFormatError (не та форма / нет list01 / нет list02-list04 в декларации) + XltxBorrowerMismatchError (parsed-INN ≠ user-INN). warn+None = всё остальное. Каждый DTO парсера получил `parse_warnings: list[str]`; VatRegistryData дополнительно `skipped_rows_count`. **Изменения:** `header_parser` все поля Optional + parse_warnings; `vat_declaration_parser` _money cells warn+None; `vat_registry_parser` _parse_row → tuple[Row\|None, reason\|None], cell-helpers warn-only, seq_no не critical (bad → 0); `snapshot_mapper.to_soliq_chunk` возвращает tuple[SoliqChunk, list[str]] (не пачкаем domain), missing INN → warn (не raise), missing year → fallback на текущий + warn; `soliq_upload` endpoint расширил response (parse_warnings + skipped_rows_count + organization_name теперь Optional); frontend `SoliqUpload` PreviewBlock рендерит collapsable жёлтый блок «Предупреждения парсера (N) · пропущено строк: M». 14 файлов / +612/−279, **+15 unit-тестов** (header +5, declaration +3, registry +5, mapper +1, endpoint +4, parser +1). Verify: ruff + mypy --strict + 443 unit + 23 integration + tsc + eslint + next build — всё зелёное. CA-014 (падение endpoint на list02!C2884 в файле папы) больше не воспроизводится по определению. На ветке `refactor/parser-best-effort` 4 коммита (этап 1+2.header → 2.declaration+registry → 3.mapper+endpoint → 4.frontend), squash в main, ветка удалена. |
| 2026-05-10 | post-3 | **Parser hardening → main** (squash `75ab42b`) | 4 атомарных fix'а перед Phase 4 в одном squash-коммите: **CA-011** ₽ → «сум» в `formatBigUzs` (4 строки в `web/.../dossier/[id]/_lib/format.ts`); **CA-012** двухуровневый детект VAT-форм для семейства 10006_* — widened substring-scan «на добавленную стоимость» + любой из markers (расчёт/расчет/сведения/декларация) по верхней зоне list01 (rows 1-20, cols A-F) case-insensitive + structural fallback (list02 G6 + list04 G7 числа). VAT_REGISTRY проверяется до VAT_DECLARATION иначе widened scan ловит подзаголовок ilova. Парсер не тронут (TODO[CA-015]); **CA-013** `getYearsRange(15)` helper в `_lib/years.ts` заменил хардкод; **CA-014** tolerant ilova parsing — row-level `MalformedXltxError` теперь WARN-лог `xltx.row_skipped sheet/row/reason` через stdlib logging + skip; `VatRegistryData.skipped_rows_count` для аудита; structural ошибки (`UnsupportedFormatError`, отсутствие листов) пробрасываются как было. **Найденный по пути bug:** мой первоначальный VAT-substring был «налог на добавленную стоимость» — не матчил реальное «налог**а** на добавленную стоимость» (родительный падеж). Сократил до «на добавленную стоимость». 7 файлов / +234/−35; 8 новых unit-тестов (4 на детектор + 4 на tolerant parsing); регрессия в `vat_declaration_parser_test.py` после CA-012 (4 теста ожидали raise, фиксы зависели от падежа) — поправил substring → 0 регрессий. Verify: ruff + mypy --strict + **428 unit** + 23 integration (5 PDF-тестов skip на Windows) + tsc + eslint + next build — зелёные. Ветка `fix/parser-hardening` удалена локально и в origin. |
